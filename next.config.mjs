/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/FTC-Scouting-App",
    output: "export",
    reactStrictMode: true,
    images: {
        unoptimized: true
    }
};

export default nextConfig;
